# rename_folder_of_images

First:

```python3
pip3 install j.osh
```

Then drop the file in a directory of images, and run it. It will create an output folder with images named 0...n.

```python3
python3 rename_img.py
```
